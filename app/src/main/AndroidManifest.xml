<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niall.electronicsstore">

    <application
        android:allowBackup="true"
        android:icon="@drawable/cpu_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cpu_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElectronicsStore">
        <activity android:name=".activities.UserPurchaseHistoryActivity"></activity>
        <activity android:name=".activities.RegisterAdminActivity" />
        <activity android:name=".activities.RegisterUserActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.BottomNavActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.RegLogActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>